#include "holberton.h"

/**
 * print_most_numbers - prints most number followd by a new line
 */
void more_numbers(void)
{
for (int i = 0; i < 10; i++)
{
int num = '0';
int diez = 0;
for (int j = 0; j < 14; j++)
{
if(diez == 1)
{
_putchar('1');
}
_putchar(num);
if(num == '9'){
num='0';
diez = 1;
}
num++;
}
_putchar('\n');
}
}
