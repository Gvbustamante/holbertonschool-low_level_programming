#include "holberton.h"
/**
 * mul - checks for uppercase letters
 * @b: the character to be checked
 * @a: the character to be checked
 * Return: a and b
 */
int mul(int a, int b)
{
return (a * b);

}
