#include "holberton.h"
/**
 * _abs - check the code for Holberton School students.
 * @r : parameter choose
 * Return: r when is positive else *
 */
int _abs(int r)
{
if (r < 0)
{
return (r * -1);
}
return (r);
}
